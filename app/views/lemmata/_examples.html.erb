<ol id="examples_lemma" class="examples">
  <%= render :partial => "example", :collection => @examples, :locals => { :exampleable => @lemma } %>
</ol>

<%= render :partial => "examples/ajax_edit" if current_user_is_admin %>

